#include<iostream>
#include<cstring>

using namespace std;
int main()
{
    string s1;
    string s2;
    cout<<"Enter two string"<<endl;
    cin>>s1;
    cin>>s2;
    int len = s1.size();
    cout<<"string 1 length is : "<<len<<endl;
}

// space is equivalent to enter in string function
// space will separate two string
// s1 and s2 is the string type object
